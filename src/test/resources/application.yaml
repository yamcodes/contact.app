spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:contacts;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
  security:
    user:
      name: admin
      password: admin
