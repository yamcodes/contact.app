spring:
  datasource:
    url: jdbc:h2:mem:contacts;DB_CLOSE_DELAY=-1
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
  security:
    user:
      name: admin
      password: admin
