export const MOCK_CONTACTS_SEED: number = 42;
export const MOCK_CONTACTS_SIZE: number = 80;

export const PAGE_SIZE: number = 10;

/** Simulated latency in milliseconds (set to 0 to disable) */
export const SIMULATED_LATENCY_MS: number = 1000;
