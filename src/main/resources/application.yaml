spring:
  application:
    name: Contacts
  thymeleaf:
    cache: true
  h2:
    console:
      enabled: true
  jpa:
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:contacts;DB_CLOSE_DELAY=-1
